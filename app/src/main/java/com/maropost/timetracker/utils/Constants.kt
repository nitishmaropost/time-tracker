package com.maropost.timetracker.utils

object Constants {

    var WEB_BASE_URL = ""

    /**
     * Enum web service request types
     */
    enum class REQUEST  {
        POST,
        PUT,
        GET
    }
}